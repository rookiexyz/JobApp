import 'package:flutter/material.dart';

class JobHorizontalTile extends StatelessWidget {
  const JobHorizontalTile({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}